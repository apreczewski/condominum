import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	body: {
		marginTop: 15,
	},

	list: {
		paddingTop: 20,
	},

	modal: {
		alignItems: 'flex-end',
		margin: 0,
	},
});

export default styles;
