import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	logo: {
		alignSelf: 'center',
		height: 150,
		marginTop: 40,
		resizeMode: 'contain',
		width: 250,
	},

	viewButton: {
		marginTop: 40,
	},
});

export default styles;
